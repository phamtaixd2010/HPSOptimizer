<UserControl x:Class="HPSOptimizer.Views.CleanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="4,10,10,10">

        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="Dọn rác &amp; giải phóng dung lượng" Style="{StaticResource H1}" />
            <TextBlock Style="{StaticResource Subtle}" Margin="0,0,0,10"
                       Text="Quét trước để biết thu hồi được bao nhiêu, rồi mới dọn. File đang bị tiến trình khác khoá sẽ tự động bỏ qua." />
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button x:Name="ScanBtn" Content="Quét dung lượng" Click="Scan_Click" />
                <Button x:Name="CleanBtn" Content="Dọn các mục đã chọn" Style="{StaticResource DangerButton}" Click="Clean_Click" />
                <Button Content="Chọn tất cả" Style="{StaticResource GhostButton}" Click="SelectAll_Click" />
                <Button Content="Bỏ chọn" Style="{StaticResource GhostButton}" Click="SelectNone_Click" />
                <TextBlock x:Name="TotalText" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="SemiBold" />
            </StackPanel>
            <ProgressBar x:Name="Progress" Visibility="Collapsed" IsIndeterminate="True" Margin="0,0,0,8" />
        </StackPanel>

        <TextBlock DockPanel.Dock="Bottom" x:Name="StatusLine" Style="{StaticResource Subtle}" Margin="0,8,0,0" />

        <DataGrid x:Name="Grid" ItemsSource="{Binding Targets}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="" Width="40"
                        Binding="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="Mục" Width="240" Binding="{Binding Name}" IsReadOnly="True" />
                <DataGridTextColumn Header="Dung lượng" Width="110" Binding="{Binding SizeText}" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="HorizontalAlignment" Value="Right" />
                            <Setter Property="Margin" Value="0,0,8,0" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Số file" Width="80" Binding="{Binding FileCount, StringFormat=N0}" IsReadOnly="True" />
                <DataGridTextColumn Header="Mô tả" Width="*" Binding="{Binding Description}" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap" />
                            <Setter Property="Margin" Value="0,4" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Cảnh báo" Width="220" Binding="{Binding Warning}" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap" />
                            <Setter Property="Foreground" Value="{StaticResource Danger}" />
                            <Setter Property="Margin" Value="0,4" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
